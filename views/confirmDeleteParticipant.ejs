<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Confirm Delete Participant</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #ffd8d1; font-family: Arial, sans-serif; padding-top: 90px; }
    .card { border-radius: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-8">
          <div class="card p-4 mb-4">
          <h2>Confirm Delete Participant</h2>
          <div class="float-end">
            <a href="/displayParticipants" class="btn btn-white btn-sm">Back</a>
          </div>
          <p class="text-muted">Please confirm you want to permanently delete this participant and their related data.</p>
        </div>

        <% if (participant) { %>
          <div class="card p-4 mb-4">
            <h4><%= participant.ParticipantFirstName %> <%= participant.ParticipantLastName %></h4>
            <p><strong>Email:</strong> <%= participant.ParticipantEmail %></p>
            <p><strong>Phone:</strong> <%= participant.ParticipantPhone || '—' %></p>
            <p><strong>City:</strong> <%= participant.ParticipantCity || '—' %></p>
            <hr />
            <form method="post" action="/deleteParticipant">
              <input type="hidden" name="participantId" value="<%= participant.ParticipantID %>">
              <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="confirm" name="confirm" required>
                <label class="form-check-label" for="confirm">I understand this action is permanent and I want to delete this participant.</label>
              </div>
              <button type="submit" class="btn btn-danger">Delete Participant</button>
              <a href="/displayParticipants" class="btn btn-secondary ms-2">Cancel</a>
            </form>
          </div>
        <% } else { %>
          <div class="alert alert-info">Participant not found.</div>
        <% } %>

      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
