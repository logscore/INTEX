<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Login Page</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet">

  <!-- Google Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&display=swap" rel="stylesheet">

  <style>
    body {
      background-color: #FFD6CF; /* Updated background color */
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .main_body {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .login-card {
      max-width: 400px;
      width: 100%;
      padding: 2rem;
      border-radius: 1rem;
      background-color: #fff;
      box-shadow: 0 0.25rem 0.75rem rgba(0, 0, 0, 0.1);
    }

    /* Option A — Soft Salmon Button */
    .btn-salmon {
      background-color: #FFD6CF;
      border: none;
      color: #000;
      font-weight: 600;
    }

    .btn-salmon:hover {
      background-color: #FFC7BD; /* Hover shade */
    }

    .footer {
      background-color: black;
      height: 100px;
      width: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    .style_guide {
      color: white;
      font-family: 'DM Serif Display', serif;
      font-weight: 400;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .copyright {
      text-align: center;
      font-size: 0.9rem;
      margin: 10px 0;
      color: #333;
    }
  </style>
</head>
<body>

  <div class="main_body">
    <div class="login-card">
      <h3 class="text-center mb-4">Login</h3>

      <% if (error_message && error_message !== "") { %>
        <h3 class="text-center mb-4 text-danger" id="error_message" name="error_message">
          <%= error_message %>
        </h3>
      <% } %>

      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="username" class="form-label">Username</label>
          <input type="text" id="username" name="username" class="form-control" required autofocus>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input type="password" id="password" name="password" class="form-control" required>
        </div>

        <div class="d-grid">
          <button type="submit" class="btn btn-salmon">Login</button>
        </div>
      </form>
    </div>
  </div>

  <div class="footer">
    <h2 class="style_guide">
      Ella
      <img src="YOUR-HEART-IMAGE-HERE.png" alt="heart" style="height: 30px;">
      Rises
    </h2>
  </div>

  <p class="copyright">© 2025 Ella Rises. All Rights Reserved.</p>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
