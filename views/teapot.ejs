<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>I'm a Teapot - HTTP 418</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    /* GLOBAL */
    body {
      background: linear-gradient(135deg, #FFD8D1 0%, #FFE5DB 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: Arial, sans-serif;
      overflow: hidden;
    }

    .teapot-container {
      text-align: center;
      position: relative;
      z-index: 2;
    }

    .status-code {
      font-size: 8rem;
      font-weight: bold;
      color: #E45858;
      line-height: 1;
      margin: 0;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
    }

    .status-message {
      font-size: 2rem;
      color: #3a3f3b;
      margin: 10px 0;
      font-weight: 600;
    }

    .teapot-gif {
      width: 300px;
      height: 300px;
      margin: 30px 0;
      filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
    }

    .description {
      font-size: 1.2rem;
      color: #555;
      max-width: 600px;
      margin: 20px auto;
      line-height: 1.6;
    }

    .fun-message {
      font-size: 1rem;
      color: #666;
      margin-top: 20px;
      font-style: italic;
    }

    .back-button {
      background: white;
      border: 2px solid #555;
      color: #555 !important;
      padding: 10px 25px;
      border-radius: 25px;
      text-decoration: none;
      display: inline-block;
      margin-top: 20px;
      transition: 0.2s;
      font-weight: 600;
    }

    .back-button:hover {
      background: #555;
      color: white !important;
    }

    /* Floating teapot animation */
    @keyframes float {
      0%, 100% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-20px);
      }
    }

    .floating {
      animation: float 3s ease-in-out infinite;
    }

    /* Particles background */
    .particles {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
    }

    .particle {
      position: absolute;
      width: 4px;
      height: 4px;
      background: rgba(228, 88, 88, 0.2);
      border-radius: 50%;
      animation: drift 10s infinite linear;
    }

    @keyframes drift {
      0% {
        transform: translate(0, 0);
        opacity: 1;
      }
      100% {
        transform: translate(100px, 100px);
        opacity: 0;
      }
    }
  </style>
</head>

<body>

<!-- Particle background -->
<div class="particles" id="particles"></div>

<!-- Teapot Container -->
<div class="teapot-container">
  <p class="status-code">418</p>
  <p class="status-message">I'm a Teapot!</p>

  <!-- Teapot Emoji Animation -->
  <div class="floating" style="font-size: 120px; margin: 30px 0;">
    🫖
  </div>

  <p class="description">
    The HTTP 418 status code is returned when a server refuses to brew coffee because it is a teapot. 
    This RFC 2324 compliant easter egg has been activated!
  </p>

  <p class="fun-message">
    ☕ → 🫖 Please use a coffee maker for coffee requests 🙏
  </p>

  <a href="/userDashboard" class="back-button">Back to Dashboard</a>
</div>

<script>
  // Generate floating particles
  function createParticles() {
    const particleContainer = document.getElementById('particles');
    for (let i = 0; i < 15; i++) {
      const particle = document.createElement('div');
      particle.className = 'particle';
      particle.style.left = Math.random() * 100 + '%';
      particle.style.top = Math.random() * 100 + '%';
      particle.style.animationDelay = Math.random() * 5 + 's';
      particle.style.animationDuration = (Math.random() * 5 + 10) + 's';
      particleContainer.appendChild(particle);
    }
  }

  createParticles();
</script>

</body>
</html>
